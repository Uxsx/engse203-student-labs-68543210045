# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** __สิรภพ กันทวี__ **รหัส:** __68543210045-9__ **Sec:** 1 **ชุด:** A

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 11.41| B1.3 | ทำไมจากข้อ B1.3 กดตัวกรอง “รอดำเนินการ” แล้วได้รายการที่ไม่ใช่ "รอดำเนินการ" อธิบายสาเหตุจากอะไรได้บ้าง | การใช้เครื่องหมายเปรียบเทียบผิด | ตรวจสอบแก้ไขการ filter ว่ามีการใช้เครื่องหมายผิดที่ตำแหน่งไหนแล้วแก้ไข|
| 11.50| B1.4 | เปิด #/requests/REQ-001 แล้วแก้ URL เป็น REQ-002 แล้วข้อมูลบนหน้าจอไม่เปลี่ยนแปลง ต้องไปดูที่ตำแหน่งไฟล์ไหน | บริเวณ useEffect เป็นจุดที่มีปัญหาเรื่องการดึงข้อมูลใหม่| เพิ่ม requestId เพื่อดึงข้อมูลมาใหม่|
| 12.21| B1.6 | ทำไมเมื่อกด Reset Demo Data แล้วจอขาวไม่มีข้อมูล | ฟังก์ชัน resetRequests เป็นแบบ Asynchronous โดยที่ในฟังก์ชัน handleReset มีการเรียกใช้และนำค่าไปใส่ setRequests ทันทีโดยไม่มีการรอค่าของข้อมูล| เพิ่มการรอข้อมูลโดยใส่ await ไว้ในอาเรย์ setRequests|
| 12.53| B2.1 | ต้องเพิ่ม input ยังไงบ้าง | เพิ่มช่อง input และ เพิ่ม state เก็บข้อความ | เพิ่ม state เพื่อเก็บข้อมูล และ เพิ่มช่องกรอก input |
| 13.11| B2.2 | ทำไมค้นหาห้องแล้วมี 3 การ์ด | เป็นเพราะระบบไปค้นเจอคำว่า "ห้อง" ในฟิลด์ สถานที่ ของ REQ-001 | แก้ไขการกรองข้อมูลแค่ ผู้แจ้ง หรือ รายละเอียดเท่านั้น|
| 13.32| B3.1 | เริ่มทำ B3.1 ยังไงบ้าง | เริ่มจากเขียน handleMarkDone สร้างฟังก์ชันและส่งต่อฟังก์ชัน สร้างปุ่มทำเสร็จ | สร้างฟังก์ชั่น handleMarkDone และ import และเพิ่มปุ่มทำเสร็จ|
| 13.35| B3.2 | ทำยังไงเมื่อกดปุ่มทำเสร็จจะเปลี่ยนสถานะเป็นเสร็จสิ้น | เมื่อกดปุ่ม ระบบจะส่ง Callback ผ่าน onMarkDone ย้อนกลับขึ้นไปหาหน้า Dashboard | แก้ไขการกดแล้วเปลี่ยนสถานะของ badge|
| 13.47| B4.1 | รับ prop priority ในไฟล์ PriorityBadge ยังไง | เขียนโค้ดเพื่อรับค่า priority และ นำเข้าคอมโพเนนต์ที่เพิ่งสร้าง และนำไปแทนที่ข้อความเดิม| แก้ไขทำไฟล์ PriorityBadge และเพิ่มโค้ดรับค่า และนำค่าไปแสดงผลในไฟล์ RequestCard|
| 13.52| B4.2 | เพิ่มสถานะ high ยังไงและให้ระบุเป็น ไม่ระบุยังไง| ปรับแต่งโค้ดให้ดักจับสถานะ | แก้ไขโค้ดเพิ่มการดักจับ priority นอกจาก urgent กับ normal|

*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** __สิรภพ กันทวี__
